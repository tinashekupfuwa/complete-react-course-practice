import React from 'react';
import ReactDOM from 'react-dom';

export class Machine extends React.Component { 
  render() { 
    function slotMachine() {
      return (
        Math.floor(math.random() * 3) + 1;
      )
    }
    icons[0] = 1;
    icons[1] = 2;
    icons[2] = 3;

/* ----------------------------- another Method ----------------------------- */

    this.props.slotChoice;
    return (
      <>

      </>
    )
  }
}